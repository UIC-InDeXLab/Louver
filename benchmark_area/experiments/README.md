The main experiments of the paper are implemented here. The main implementations of our indexing algorithm is in `benchmark_area/kernel_impl`. Other insights are in `benchmark_area/*` directories.

List of experiments:
1- Showing the bad effect of false negativity
2- Score distribution. How many points are actually in the tail for a long output reasoning task?